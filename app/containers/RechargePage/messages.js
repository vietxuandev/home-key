/*
 * RechargePage Messages
 *
 * This contains all the text for the RechargePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RechargePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RechargePage container!',
  },
});
