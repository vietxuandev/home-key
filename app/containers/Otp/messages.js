/*
 * Otp Messages
 *
 * This contains all the text for the Otp container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Otp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Otp container!',
  },
});
