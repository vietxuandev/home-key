/*
 * MotelRoom Messages
 *
 * This contains all the text for the MotelRoom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MotelRoom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MotelRoom container!',
  },
});
