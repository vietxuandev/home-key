// import { selectPaymentReturnDomain } from '../selectors';

describe('selectPaymentReturnDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
