/*
 * PaymentReturn Messages
 *
 * This contains all the text for the PaymentReturn container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PaymentReturn';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PaymentReturn container!',
  },
});
