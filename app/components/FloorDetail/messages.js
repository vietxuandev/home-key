/*
 * FloorDetail Messages
 *
 * This contains all the text for the FloorDetail component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FloorDetail';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FloorDetail component!',
  },
});
