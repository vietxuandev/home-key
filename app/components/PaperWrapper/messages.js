/*
 * PaperWrapper Messages
 *
 * This contains all the text for the PaperWrapper component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PaperWrapper';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PaperWrapper component!',
  },
});
