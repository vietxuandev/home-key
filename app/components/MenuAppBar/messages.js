/*
 * MenuAppBar Messages
 *
 * This contains all the text for the MenuAppBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MenuAppBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MenuAppBar component!',
  },
});
