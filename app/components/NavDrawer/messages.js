/*
 * NavDrawer Messages
 *
 * This contains all the text for the NavDrawer component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.NavDrawer';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NavDrawer component!',
  },
});
